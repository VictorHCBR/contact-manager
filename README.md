# Passo-a-Passo

git clone
cd pasta-projeto
docker compose up --build

Ou se não quiser usar o docker basta ter o SQL Server instalado e colocar as credenciais locais na sua ConnectionString e executar pelo Visual Studio mesmo.
